[package]
name = "keyboard-recorder"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
strip = true
codegen-units = 1

[dependencies]
device_query = "4.0.1"